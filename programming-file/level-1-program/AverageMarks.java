import java.util.*;
// Program to find average percent mark of Sam in PCM
class AverageMarks {
    public static void main(String[] args) {

        // Declare and assign marks in 3 subjects
        int maths = 94;
        int physics = 95;
        int chemistry = 96;

        // Calculate average
        double average = (maths + physics + chemistry) / 3.0;

        // Display average marks
        System.out.println("Sam’s average mark in PCM is " + average);
    }
}
